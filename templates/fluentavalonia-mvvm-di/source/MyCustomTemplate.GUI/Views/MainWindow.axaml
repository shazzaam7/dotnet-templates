<windowing:FAAppWindow xmlns="https://github.com/avaloniaui"
                       xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                       xmlns:vm="using:MyCustomTemplate.GUI.ViewModels"
                       xmlns:views="clr-namespace:MyCustomTemplate.GUI.Views"
                       xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                       xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                       xmlns:windowing="using:FluentAvalonia.UI.Windowing"
                       xmlns:controls="using:FluentAvalonia.UI.Controls"
                       mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="600"
                       x:Class="MyCustomTemplate.GUI.Views.MainWindow"
                       x:DataType="vm:MainWindowViewModel"
                       Icon="/Assets/icon.ico"
                       Title="{Binding WindowTitle}">

    <Design.DataContext>
        <vm:MainWindowViewModel />
    </Design.DataContext>
    <Grid>
        <views:MainView />
        <controls:FAInfoBar x:Name="InfoBar"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Top"
                            IsClosable="True" />
    </Grid>
</windowing:FAAppWindow>